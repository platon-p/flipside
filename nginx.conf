events {
    worker_connections 1024;
}
http {
    server {
        listen 80;

        location /api/auth {
            proxy_pass http://authservice:80;
        }

        location ~ ^/api/(cardset|cards) {
            proxy_pass http://cardservice:80;
        }
    }
}
